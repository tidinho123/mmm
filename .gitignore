# Arquivos que o Python cria sozinho ao rodar o bot (não precisa guardar)
__pycache__/
*.pyc

# Seus dados secretos (token do Telegram). NUNCA sobe pro repositório.
MEUS_DADOS.py

# Perfil do Chrome do bot Bac Bo (guarda o SEU login). NUNCA sobe.
perfil_bacbo_chrome/

